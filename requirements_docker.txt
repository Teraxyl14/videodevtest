# Project Aether v2.2 - Docker Container Requirements
# Installed INSIDE the NGC PyTorch 26.03 container
#
# NOTE: torch, torchvision, torchaudio are installed explicitly in Dockerfile
#   (PyTorch 2.10.0 cu128 wheel — required by vLLM 0.18.0)
#
# NOTE: vLLM 0.18.0 is installed explicitly in Dockerfile
#
# NOTE: crawl4ai, langgraph, pydantic-ai, google-genai, easytranscriber,
#   easyaligner, pyannote-audio are installed separately in the Dockerfile
#   with --no-deps to avoid transitive dependency conflicts.

# HuggingFace ecosystem (already installed in Dockerfile, these are minimums)
huggingface_hub>=1.0.0
transformers>=5.2.0

# Video/Image processing
pysubs2
filterpy==1.4.5

# GPU-accelerated transforms
kornia
librosa
skia-python

# Object tracking (YOLO26s-Pose)
ultralytics>=8.4.0

# GPU monitoring
nvidia-ml-py

# Dashboard Server
fastapi
uvicorn[standard]
websockets

# Trend Discovery — crawl4ai runtime deps (crawl4ai --no-deps in Dockerfile)
beautifulsoup4>=4.12
playwright
playwright-stealth
aiohttp>=3.9
html2text>=2024.2
tiktoken
rank-bm25

# Google APIs
google-api-python-client
pytrends
yt-dlp
python-dotenv

# Orchestration — runtime deps for --no-deps packages
httpx>=0.27.0
langsmith>=0.1.0
langchain-core>=0.3.0
griffe>=1.0.0
langgraph-checkpoint
pydantic-ai-slim>=0.0.36
google-genai>=0.8.0

# Crawl4AI runtime deps
pyOpenSSL
aiofiles
snowballstemmer
fake-useragent
chardet
aiosqlite

# OpenAI client (for vLLM sidecar OpenAI-compatible API)
openai>=1.60.0

# Data pipeline / Checkpointing (Redis Streams)
redis

# easytranscriber runtime deps
ctranslate2>=4.5.0
faster-whisper>=1.1.1
nltk>=3.9.1
omegaconf>=2.3.0
