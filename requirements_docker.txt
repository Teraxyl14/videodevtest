# AI Video Factory - Docker Container Requirements (Stage 1: Core)
# Installed INSIDE the NGC PyTorch 25.03 container
#
# NOTE: The NGC base already provides: torch, torchvision, torchaudio,
#   numpy, scipy, pillow, opencv, grpcio, protobuf, pydantic, safetensors
#   DO NOT list those here.
#
# NOTE: pyannote-audio, whisperx, and autoawq are installed separately
#   in the Dockerfile with --no-deps due to torch/numpy version pins.

# HuggingFace ecosystem
huggingface_hub<1.0.0
transformers>=4.48.0,<5.0.0

# Video/Image processing
pysubs2
filterpy==1.4.5

# GPU-accelerated transforms
kornia
librosa
skia-python

# Object tracking
ultralytics>=8.4.0

# Qwen VL utilities
qwen-vl-utils

# GPU monitoring
nvidia-ml-py

# gRPC tooling
grpcio-tools

# Dashboard Server
fastapi
uvicorn[standard]
websockets

# Trend Discovery
curl_cffi
pytrends
playwright
playwright-stealth
google-genai
google-api-python-client
yt-dlp
python-dotenv

# 4-bit quantization
bitsandbytes>=0.46.1

# Legacy Gemini SDK
google-generativeai

# Data pipeline
redis

# WhisperX runtime deps that DON'T conflict with NGC
ctranslate2>=4.5.0
faster-whisper>=1.1.1
nltk>=3.9.1
omegaconf>=2.3.0
